name: funding-arb
version: 0.1.0
enabled: true           # flip to true and disable noop when ready
poll_interval: 10s
timeout_ratio: 0.8
budget: 200              # USD notional cap for this skill's open exposure
slots: 1
margin_per_slot: 100     # USD margin per slot
env:
  - STRATEGY_API_BASE
  - STRATEGY_API_KEY
